import db from '../helpers';

export const getMessages = async (coduser) => {};

export const getMessage = async (codmessage) => {};

export const newMessage = async (message) => {};

export const deleteMessage = async (codmessage) => {};

